@using CheeseMVC.Models
@model List<Cheese>

<h1>MyCheeses</h1>

<p><a asp-controller="Cheese" asp-action="Add">Add Cheese</a></p>

@if (Model.Count == 0)
{
    <p>No cheeses yet!</p>
}
else
{
    <table class="table">
        <thead>
        <tr>
            <th>Name</th>
            <th>Description</th>
            <th>Type</th>
            <th>Rating</th>
            <th>Options</th>
        </tr>
        </thead>
        <tbody>
        @foreach (Cheese cheese in Model)
        {
            <tr>
                <td>@cheese.Name</td>
                <td>@cheese.Description</td>
                <td>@cheese.Type</td>
                <td>@cheese.Rating</td>
                <td>
                    (
                    <a asp-controller="Cheese"
                       asp-action="RemoveSingleCheese"
                       asp-route-cheeseId="@cheese.CheeseId">delete</a>
                    &nbsp;|&nbsp;
                    <a asp-controller="Cheese"
                       asp-action="Edit"
                       asp-route-cheeseId="@cheese.CheeseId">edit</a>
                    )
                </td>
            </tr>
        }
        </tbody>
    </table>
}